"""api package -- routes, services and schemas for the Bari Anemia Screening API.

This package provides API route definitions (`routes.py`),
business logic (`services.py`) and pydantic models (`schemas.py`).
"""
