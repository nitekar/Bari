[pytest]
pythonpath = .
markers =
	unit: unit tests (pure functions/helpers; no external I/O)
	validation: data/model/schema validation tests
	integration: integration tests (API routes/pipeline wiring; may use TestClient)
	uat: user acceptance tests (typically run against deployed services)